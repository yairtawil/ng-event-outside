import { EventOutsideDirective } from './ng-event-outside.directive';

describe('NgEventOutsideDirective', () => {
  it('should create an instance', () => {
    const directive = new EventOutsideDirective();
    expect(directive).toBeTruthy();
  });
});
